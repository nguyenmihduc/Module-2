
export class Absolute {
    static findAbsolute(x: number) {
        if (x > 0) {
            return x;
        } else if (x < 0) {
            return x;
        } else {
            return 0
        }
    }
}